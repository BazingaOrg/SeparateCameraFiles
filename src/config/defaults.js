/**
 * 默认配置文件
 */

// 默认源路径
const DEFAULT_SOURCE_PATH = '/Volumes/Bazinga 12TB/Photos/JPG';

// 默认文件类型
const DEFAULT_FILE_TYPE = 'RW2';

module.exports = {
    DEFAULT_SOURCE_PATH,
    DEFAULT_FILE_TYPE
}; 